<h1>Editing Image</h1>

<% form_for(@image, :html => { :multipart => true }) do |f| %>
  <fieldset>
    <%= render :partial => 'form', :locals => { :f => f } %>
    <div class='bw'>
      <%= f.submit 'Update' %> - or - <%= link_to 'Cancel', images_path %>
    </div>
  </fieldset>
<% end %>