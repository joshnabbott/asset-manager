<h1>Editing Video</h1>

<% form_for(@video, :html => { :multipart => true }) do |f| %>
	<fieldset>
		<%= render :partial => 'form', :locals => { :f => f } %>
		<div>
    		<%= f.submit 'Update' %>
  		</div>
	</fieldset>
<% end %>

<%= link_to 'Show', @video %> |
<%= link_to 'Back', videos_path %>